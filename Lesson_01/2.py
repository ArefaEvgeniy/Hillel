# import langdetect
from langdetect import detect


# lang1 = detect("War doesn't show who's right, just who's left.")
# lang2 = detect("Ein, zwei, drei, vier")
#
# print(lang1)
# print(lang2)

print(detect("War doesn't show who's right, just who's left."))
print(detect("Ein, zwei, drei, vier"))
