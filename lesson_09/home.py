# При помощи PIP установить такие пакеты:
# - requests (версии 2.26.0);
# - lxml (последней версии).
# Записать установленные зависимости в файл requirements.txt.
# Удалить эти зависимости по одной и затем установить их из файла requirements.txt.
# Удалить все зависимости одной командой используя файл requirements.txt,
# при этом удаление должны быть автоматическим без каких-либо подтверждений.
#
# Для проверки прислать файл requirements.txt и перечень выполненных действий.
