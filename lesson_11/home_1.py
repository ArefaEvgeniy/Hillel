# Створити словник як ключ якого буде 6-ти значне число,
# а в якості значень кортеж що складається з 2-х елементів - ім'я (str),
# вік (int). Зробити близько 5-6 елементів словника. Записати цей словник
# на диск у json-файл.

import json
data = {111111: ('Sam', 42), 100003: ('Tom', 12), 222043: ('Adam', 69),
        679432: ('Jasy', 25), 980451: ('Kim', 33), 666740: ('Jasy', 15)}

with open('file_json.json', 'w') as file_json:
    json.dump(data, file_json)
